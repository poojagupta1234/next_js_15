
import LoginBlink from '@/app/components/ecom/login/login';
/**
 * LoginPage component handles the user login process.
 */
export default function LoginPage() {
  /**
   * handleLogin function sends login data to the server and manages the response.
   * @param {Object} loginData - The login data containing user's email and password.
   */

  return (
    <div>
    
      <LoginBlink />
    </div>
  );
}
